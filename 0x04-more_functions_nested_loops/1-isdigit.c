#include "main.h"

/**
* _isdigit - checks for upper case
* @c: number to be checked
* Return: 1 or 0 depends
*/

int _isdigit(int c)
{
	if ((c >= '0' && c <= '9'))
	{
	return (1);
	}

	return (0);
}
